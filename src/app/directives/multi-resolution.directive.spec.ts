import { MultiResolutionDirective } from './multi-resolution.directive';

describe('MultiResolutionDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiResolutionDirective();
    expect(directive).toBeTruthy();
  });
});
